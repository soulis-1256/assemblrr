services:
  # Jellyfin — open source media server (recommended)
  jellyfin:
    profiles: ["jellyfin"]
    image: lscr.io/linuxserver/jellyfin
    container_name: jellyfin
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VERSION=docker
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/jellyfin:/config
    ports:
      - 8096:8096
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8096/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.11

  # Emby — media server with premium features
  emby:
    profiles: ["emby"]
    image: lscr.io/linuxserver/emby
    container_name: emby
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VERSION=docker
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/emby:/config
    ports:
      - 8096:8096
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8096/emby/System/Ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.11

  # Plex — popular media server with advanced features (uses host networking)
  plex:
    profiles: ["plex"]
    image: lscr.io/linuxserver/plex
    container_name: plex
    network_mode: host
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VERSION=docker
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/plex:/config
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:32400/identity"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  # qBittorrent — torrent client (ports added by overlay)
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - WEBUI_PORT=8081
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/qbittorrent:/config
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8081"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Sonarr — TV show management and automation
  sonarr:
    image: lscr.io/linuxserver/sonarr
    container_name: sonarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/sonarr:/config
      - ${INSTALL_DIRECTORY}/scripts:/scripts:ro
      - ${INSTALL_DIRECTORY}/secrets:/run/secrets:ro
    ports:
      - 8989:8989
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8989/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.13

  # Radarr — movie management and automation
  radarr:
    image: lscr.io/linuxserver/radarr
    container_name: radarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/radarr:/config
      - ${INSTALL_DIRECTORY}/scripts:/scripts:ro
      - ${INSTALL_DIRECTORY}/secrets:/run/secrets:ro
    ports:
      - 7878:7878
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:7878/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.14

  # Prowlarr — indexer management for *arr apps
  prowlarr:
    image: lscr.io/linuxserver/prowlarr
    container_name: prowlarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${INSTALL_DIRECTORY}/config/prowlarr:/config
    ports:
      - 9696:9696
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9696/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.15

  # Seerr — media request management (successor to Jellyseerr/Overseerr)
  # Host port 5055 is published by seerr-gateway (not seerr) so DELETE /request
  # can cascade into media file purge. Seerr stays on the Docker network only.
  # Retirement: see docs/seerr-delete-request.md
  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: seerr
    init: true
    environment:
      - TZ=${TZ}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${INSTALL_DIRECTORY}/config/seerr:/app/config
    expose:
      - "5055"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5055/api/v1/settings/public"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.17

  # Thin reverse proxy in front of Seerr: pass-through + delete-request purge.
  # TV: season-scoped via Sonarr when the request is a subset of remaining seasons.
  # SEERR_DELETE_REQUEST_PURGE=0 disables intercept (stock Seerr semantics).
  seerr-gateway:
    image: python:3.12-alpine
    container_name: seerr-gateway
    environment:
      - TZ=${TZ}
      - SEERR_UPSTREAM=http://seerr:5055
      - SEERR_GATEWAY_LISTEN=0.0.0.0
      - SEERR_GATEWAY_PORT=5055
      - SEERR_DELETE_REQUEST_PURGE=${SEERR_DELETE_REQUEST_PURGE:-1}
      - SONARR_URL=http://sonarr:8989
      - SONARR_CONFIG=/config/sonarr/config.xml
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${INSTALL_DIRECTORY}/scripts/seerr-gateway.py:/scripts/seerr-gateway.py:ro
      # Bind the dir, not config.xml — Docker creates a directory if the file is missing.
      - ${INSTALL_DIRECTORY}/config/sonarr:/config/sonarr:ro
    ports:
      - 5055:5055
    depends_on:
      seerr:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python3",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5055/_assemblrr/ready', timeout=5)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    command: ["python3", "/scripts/seerr-gateway.py"]
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.24

  # Recyclarr - automatically syncs TRaSH guides
  recyclarr:
    image: ghcr.io/recyclarr/recyclarr:latest
    container_name: recyclarr
    user: ${PUID}:${PGID}
    environment:
      - TZ=${TZ}
      - CRON=@daily
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${INSTALL_DIRECTORY}/config/recyclarr:/config
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.18

  # Deletes under /data/media → purge *arr entry + qB torrent/files
  media-purge-watch:
    image: assemblrr/media-purge-watch:local
    build:
      context: ${INSTALL_DIRECTORY}/compose/sidecars
      dockerfile: media-purge-watch.Dockerfile
    container_name: media-purge-watch
    environment:
      - TZ=${TZ}
      - MEDIA_ROOT=/data
      - CONFIG_DIR=/config
      - RADARR_URL=http://radarr:7878
      - SONARR_URL=http://sonarr:8989
      - AUTH_USERNAME_FILE=/run/secrets/auth_username.txt
      - AUTH_PASSWORD_FILE=/run/secrets/auth_password.txt
      - MEDIA_PURGE_WATCH=${MEDIA_PURGE_WATCH:-1}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config:/config:ro
      - ${INSTALL_DIRECTORY}/scripts:/scripts:ro
      - ${INSTALL_DIRECTORY}/secrets:/run/secrets:ro
    command: ["/scripts/media-purge-watch.sh"]
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.23

  # Bazarr — automatic subtitle management (Sonarr/Radarr companion)
  bazarr:
    image: lscr.io/linuxserver/bazarr
    container_name: bazarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${MEDIA_DIRECTORY}:/data
      - ${INSTALL_DIRECTORY}/config/bazarr:/config
    ports:
      - 6767:6767
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:6767/api/system/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      assemblrr_network:
        ipv4_address: 172.60.0.22

networks:
  assemblrr_network:
    name: assemblrr_network
    ipam:
      config:
        - subnet: 172.60.0.0/24
